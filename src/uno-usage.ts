/**
 * UnoCSS 原子类白名单（技术栈预留）。
 * 项目页面样式均已用 TDesign 变量手写，暂不依赖 UnoCSS 原子类。
 * 若后续要用，在此文件加入对应 class 字符串（如 'flex', 'items-center'），
 * 再 `npm run uno` 生成到 styles/uno.wxss 并从 app.wxss 引入。
 */
export const unoUsage = ''
