/**
 * 卡片容器（TDesign 变量自建）。
 * 纯展示型组件，无事件。
 */
Component({
  properties: {
    /** 是否带柔和阴影 */
    shadow: { type: Boolean, value: false },
    /** 是否带内边距 */
    padded: { type: Boolean, value: false },
  },
})
