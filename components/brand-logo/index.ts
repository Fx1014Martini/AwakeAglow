/**
 * 品牌 Logo（对齐原型 assets/icons/logo.svg，内联 SVG，stroke/fill currentColor 跟随主题）。
 */
Component({
  properties: {
    /** 宽度（rpx），高按 310:96 比例 */
    width: { type: Number, value: 235 },
    mode: { type: String, value: 'coffee' },
  },
})
