# 部署日志 · 2026-08-17

## 目标
小程序前后端 + 数据库在微信云托管上一键部署上线。

## 今日完成事项

### 1. 前端配置切换
- `apiBaseUrl` 从本地 `http://127.0.0.1:8010/api/v1` → 自定义域名 → 云托管默认域名
- 最终值：`https://flask-8rcf-296365-11-1467877005.sh.run.tcloudbase.com/api/v1`
- 原因：`cfx.woofcloud.com` 未配置 SSL 证书，HTTPS 握手失败（证书返回 `*.tcloudbase.com`）

### 2. BFF 目录隔离
- `project.config.json` 增加 `packOptions.ignore` 排除 `bff/` 目录
  - 消除微信开发者工具扫描 `bff/__pycache__` / `.pytest_cache` 产生的 ENOENT 警告
- 清空 `cloudbaseRoot` 字段（之前误设为 `bff/` 导致云托管插件生成额外目录）

### 3. pytest 缓存占位
- `bff/pytest-cache-files-e1kwxz41/` 目录被开发者工具文件监听器记住
- 删除后导致真机调试/上传时报 ENOENT
- 临时方案：重新创建占位文件（README.md / .gitignore / CACHEDIR.TAG）
- `.gitignore` 新增 `pytest-cache-files-*/` 规则避免提交

### 4. 域名连通性诊断
| 域名 | DNS | HTTP | HTTPS | 备注 |
|---|---|---|---|---|
| `awakeaglow.asia` | ✅ | - | ❌ | 证书不匹配 |
| `cfx.woofcloud.com` | ✅ | ✅ 200 | ❌ TLS | 无 SSL 证书 |
| `flask-8rcf-...tcloudbase.com` | ✅ | ✅ 200 | ✅ 200 | **当前使用** |

### 5. 代码审查修复（commit 05a4845）
- `apiBaseUrl` 误留本地 HTTP 地址 → CRITICAL 已修复
- `import random` 函数内导入（2 处）→ MINOR 已提到顶部
- `recommend()` reasons 重复计算 → MINOR 已缓存 d_attrs/d_scenes
- 13 个 BFF 单测全部通过

### 6. 首页图片显示修复（commit 35368ef）
- BFF `featured_summaries()` 补 `posterUrl` 字段
- 前端 `normalizeImageUrl` 增加 wikimedia `Special:Redirect` 检测（301 重定向微信无法加载）
- 移除无效的 `baseUrl` 图片代理转换

## 待办事项（需用户在控制台操作）

### BFF 服务部署
1. 登录 https://cloud.weixin.qq.com
2. 新建服务：名称 `awakeaglow-bff`，代码源 GitHub `Fx1014Martini/AwakeAglow`，**根目录 `bff/`**
3. 监听端口 80，CPU 0.5 核，内存 1G
4. 创建并开启 MySQL（云托管自动注入 MYSQL_ADDRESS/USERNAME/PASSWORD）
5. 部署成功后验证 `/api/v1/bootstrap` 返回 JSON

### 数据库种子数据导入
- 云托管控制台 → 数据库 → 导入 `bff/seed.sql`（384 drink + 219 taxonomy + 3840 drink_similar + 6 meta）

### 小程序后台域名白名单
- https://mp.weixin.qq.com → 开发管理 → 开发设置 → 服务器域名
- `request 合法域名` 添加：`https://flask-8rcf-296365-11-1467877005.sh.run.tcloudbase.com`

### 自定义域名 + SSL 证书（可选，长期方案）
- 如需使用 `cfx.woofcloud.com`，需申请 SSL 证书并在云托管控制台绑定自定义域名

## 验证清单
- [ ] BFF 服务运行中
- [ ] `/api/v1/bootstrap` 返回 200 JSON
- [ ] `/api/v1/drinks?mode=coffee` 返回饮品列表
- [ ] 小程序首页数据正常加载
- [ ] 小程序首页海报图片正常显示
- [ ] 真机调试通过

## 本次提交
- commit: 待生成
- 改动：
  - `config/app-config.ts`: apiBaseUrl 切换为云托管默认域名
  - `docs/部署日志-2026-08-17.md`: 新增部署日志
